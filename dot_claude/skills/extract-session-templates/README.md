# extract-session-templates

One-time bootstrap that analyzes real Claude session `.jsonl` files under
`~/.claude/projects/` and produces an archetype template library at
`~/.claude/data/session-templates/` consumed by the `generate-mock-sessions`
skill.

Implementation in progress.
