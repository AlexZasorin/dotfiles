# Real Claude Session Record Schema

Reference for what each record type looks like in real `~/.claude/projects/**/*.jsonl` files. Used to verify scaffolder output against reality.

Stub — populated during Phase 5.
