# generate-mock-sessions

Per-run generator that fabricates plausible Claude Code session `.jsonl` files
for training a local content-aware model. Consumes the archetype template
library produced by `extract-session-templates` from
`~/.claude/data/session-templates/`.

Implementation in progress.
